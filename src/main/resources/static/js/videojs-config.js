(function () {
    // Video.js normally injects a <style> element for player dimensions. The
    // programme player is sized by app.css, so keep dynamic style elements off.
    window.VIDEOJS_NO_DYNAMIC_STYLE = true;
}());
